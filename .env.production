# 生产构建期注入前端的 Convex 后端地址。
# 这不是密钥——它会打进客户端产物，任何访客都能在浏览器里看到，Convex 的
# 访问控制靠函数自身的 public/internal 区分而非隐藏 URL。
# 放在版本控制里而不是 Vercel 面板，是因为面板状态会悄悄陈旧：本次部署前
# 面板上存的还是上一个 dev 部署的地址，差点让线上连错后端。
VITE_CONVEX_URL=https://diligent-mule-913.convex.cloud
