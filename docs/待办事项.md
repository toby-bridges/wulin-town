# 武林小镇 (wulin-town) - 待办事项

> 最后更新：2026-09-01（G1/G2 游戏 bug 修复上线后）
> 上一版（2026-01-07）里的项目已全部完成或作废，见文末「已归档」。

---

## 已完成（2026-09-01 夜）：游戏 bug 扫描 + G1/G2 修复 ✅

一轮系统性 bug 扫描（代码 + 生产日志 + 生产数据三路取证），查出 6 条，今天修掉前两条最严重的。
详细证据链见 PIT-0016 / PIT-0017。

**G1 · 玩家 5 分钟被无条件踢出**（已修）
`lastInput` 全仓库只有一处写入（`Player.join` 内），9 个 inputHandler 一个都不更新它 →
「闲置超时」从来没度量过闲置，它度量的是「加入至今」。修法：`notePlayerActivity` 挂在
`Game.handleInput`（输入分发的唯一入口），任何未来新增输入自动生效。口径按用户裁定：
只认主动操作（移动/对话），页面开着不算。
连带影响：修好后 `playerDescriptions` 的增长从「每人每 5 分钟一行」降到「每个新访客一行」，
那本来就是设计预期。**注意：那些垃圾行不能删** —— `messages.ts:22` 找不到描述行时是 throw，
`Messages.tsx` 也靠它显示历史对话里的人名，删行会让消息历史直接报错。要清得先改这两个消费者。

**G2 · LLM 一抖动角色冻结 120 秒**（已修）
`agentGenerateMessage` 的 `completionFn` 没 try/catch，抛出后 `inProgressOperation` 只能等
`ACTION_TIMEOUT` 清，那 120 秒里角色完全惰性。修法：try/catch + 新增 `agentAbandonMessage`
回执（清操作位 + 停对话 + 释放打字锁），刻意不原地重试（'start' 分支对发起方无退避，会 1 秒
一次捶 API）。`agentRememberConversation` 同一个洞一并补上。
顺带修掉一条既有缺陷：`Conversation.stop` 原本无条件设 `toRemember`，而零消息的对话从未进过
`archivedConversations`（`saveDiff` 按前后两版 world 的差集归档），于是 `rememberConversation`
必抛 `Conversation ... not found` —— `rejectInvite` 是这条路上最早的例子。

**附带解锁：jest 能测 aiTown 层了**
`jest.config.ts` 补上 `.js` 的 transform。此前任何 import 链碰到 `convex/_generated/*.js`（ESM）
的模块（game.ts / world.ts / agent.ts …）都以 "Cannot use import statement outside a module" 失败
——这就是本仓测试长期只能覆盖纯数据模块的原因。现在 115 条测试全绿，且没拖慢（0.29s）。

---

## 剩下的游戏 bug（按严重度，未修）

**G7 · 角色把会合点算到墙里，然后每 tick 重试 60 秒** 🔴 新发现，未修
dev 日志实测：`Failed to route to {"x":45,"y":41}` 在一秒内刷 75 次，两个角色
（`Agent p:4 walking towards p:16` / 反向）盯着同一个目标点。查证 `data/collisionMap.txt`
第 41 行第 45 列是 `#` —— 会合点在墙里。
机制：`agent.ts` 的 `walkingOver` 分支取两人坐标中点当目的地，不检查是否可走；
`findRoute` 在 `bestCandidate.length === 0` 时直接 `return null`（`movement.ts:141-147`），
没有「退而求其次找个附近可走格」的步骤；`movePlayer` 失败后清掉 pathfinding，
下一 tick 又算出同一个墙里的中点 → 紧密循环，直到 `INVITE_TIMEOUT`(60s) 放弃。
**观感上和 G2 一模一样（角色站着不动），但成因完全不同。**
证据强度要说清：目前只有 dev 上一对角色的一次实测，**没有测过「中点落在墙里」的发生频率**。
它是否构成「小镇看着冷清」的实质原因还是个待验假设——下次动手前先埋一个计数器数一数。
值得排到下一批第一位（成因已确证，只是量级未知）。
修法方向：中点落在挡路格时就近找可走格（`patchMapCollision` 里那套 BFS 可以复用），
或给 `findRoute` 的失败加退避而不是每 tick 重试。

**G3 · 玩家主动搭话，AI 面对面站着 60 秒不说话** 🔴
人类是 `creator`（`PlayerDetails.tsx:96` → `conversation.ts:142`），AI 无条件接受邀请后走过来，
但 `agent.ts:167-171` 规定非发起方要等 `started + AWKWARD_CONVERSATION_TIMEOUT`(60s) 才开口。
游戏隐含要求人类先开口，界面零提示。

**G4 · 三个超时常量跑的是调试档** 🟡（取舍问题）
`constants.ts` 自己的注释写明 active 行是 "more time for local dev"，注释掉的那行是 "normally fine"：
ACTION_TIMEOUT 120s(vs 60s)、AWKWARD_CONVERSATION_TIMEOUT 60s(vs 20s)、
MAX_CONVERSATION_DURATION 10min(vs 2min)。前两个直接放大 G2 残余影响和 G3 的沉默；
第三个实际很少生效（`MAX_CONVERSATION_MESSAGES = 8` 几乎总是先触发）。
**注意：收紧 ACTION_TIMEOUT 只是减半 G2 的残余暴露面，不是 G2 的修复——G2 已经从根上修了。**

**G5 · 人类交互面板整片英文** 🟡 13 处，全在核心路径
`PlayerDetails.tsx`: Start conversation / Waiting for accept... / Walking over... / Leave conversation /
Accept / Reject / This is you! / Conversing with you! / Previous conversation
`Messages.tsx`: typing... / `{name} joined the conversation.`（两处）
`MessageInput.tsx:92`: placeholder="Type here"
`player.ts` 的 `Only 8 human players allowed at once.` 会经 toast 原样弹给中文用户。

**G6 · 人类在 AI 占着打字锁时打字，输入指示器被静默跳过** 🟢 纯观感
`MessageInput.tsx:34-36` 直接 return（注释明写 "Don't show a toast on error"）。消息本身仍能发出。

---

## ⚠️ 运营警报（升级为实锤结论，2026-09-01）

AstraFlow（api.modelverse.cn）账户 403 `account overdue` 的复发模式已锁定：
- 8/28：欠费 → 充值 → 数小时后复发；
- 9/1：12:12 探测正常 → 13:08 复发，**期间近零消费**（两个世界全程沉睡）。
钱不是用完的，是**账户状态自己翻回欠费**——账上有一张未结清订单在周期性重新锁定账户，
充余额只能临时解锁。**行动**：控制台单独结清「未支付订单」，搞不定拿 trace_id 找客服：
`323eebf0-dda4-4b7b-9a4a-3dd0440d95ec`。
结清后无需任何操作：下一个访客进站 10 秒内自动补产事件（v1.3 供给保活），当晚 23:00 回顾把积攒事件说成一回书。

---

## 事故复盘（2026-09-01 晚）：两个"疑似 v1.3 回归"均排除，未回滚

用户报告：右侧滚动条拖不动 + 角色全体静止。逐一定位：
- **角色静止** = 生产世界处于 `stoppedByDeveloper`（顶栏「暂停」按钮写入的状态，疑似误触）。
  该状态**按设计**连访客心跳都不唤醒，与 v1.3 无关。已 `testing:resume --prod` 恢复，
  引擎推进实证（10 秒 6 步）。改进想法（未排期）：暂停按钮加确认或更醒目的恢复提示。
- **滚动条失效** = 说书人正文滚动区 `max-h-[40vh]` 的 vh 在受限视口环境解析为 0 →
  容器塌为零高 → 滚动条轨道消失。**对照实验**：v1.2 旧部署 URL 测得逐字节相同的塌陷值
  （maxHeight:0px），证明是 v1.2 上线即有的隐患而非 v1.3 回归——正文变长后才被触发。
  已修（fceb439）：40vh→16rem 固定高度，构造性免疫视口单位，线上实证轨道恢复（256px 可拖）。

方法论：判"要不要回滚"先做旧版本对照实验，同一缺陷在旧版复现 = 回滚无效，向前修。

---

## 已完成（2026-09-01）：供给保活 + 小修打磨 v1.3 ✅（cbea2d5 + 5e61d81）

治 v1.2 后四天断粮（无访客→世界休眠→编剧跳过→回顾无材料也跳过的级联）+ 清 6 件小账：

- **唤醒补产**：访客把沉睡世界唤醒时，最新事件超 2 小时就 10 秒后自动补产一条（dev 实测链路到 LLM 边界全通）
- **回顾窗口**改"自上次回顾以来"（有界读 61 条防自锁），回目序号由代码注入（第 N 回=已有回顾数+1），不再 LLM 自编
- sanitize 截断改按字符（不劈 emoji，66,429 用例穷举验证）；llm 日志止血（不再打全量 prompt）
- **hooks lint 护栏上岗**（rules-of-hooks=error 0 违规；9 条既有 exhaustive-deps warn 入档，其中 3 条标注"照提示修必回归"禁 autofix）
- 说书人开场键盘可达（Esc/自动聚焦/最小焦点陷阱——修复了滚动区场景主按钮 Tab 不可达的 Important 缺陷）；长驻访客读过新回顾不再重演开场

流程：SDD 三层审查，全程 1 Important（已修复核销）/0 Critical；终审 13+2 条 deferred 全裁 DEFER-OK。
**v1.4 候选**（终审携带）：题卡由"introDone 门控挂载"改"门控显示"（首访客读开场>30s 时补产事件不弹题卡的缝隙）；
回顾在途窗口的廉价修法（since=max(eventIds.startTime)，非 schema 变更）；Character.tsx 贴图刷新疑似 bug（任务卡已开）。

---

## 已完成（2026-08-28）：剧情可观察性 v1.2 ✅（第一档 434a0c8 + 第二档 b08504a）

解决"功能在跑但访客感知为零"：内容节奏（事件 30 分钟/条、回顾 1 天/回）与访客停留（分钟级）
错配，设计从"等新内容"改为"任何时刻进来当前剧情糊在脸上"。四件套：

- **📜 事件横幅**（EventBanner）：常驻画布上方显示最新事件，点击直开大事记
- **🔴 未读红点**：大事记按钮右上角；水位线用服务端时间戳（免疫本地时钟偏移），模态框开着期间持续推进；sr-only 读屏可感
- **⚡ 章回题卡**（EventTitleCard）：新事件落库 0.7s 弹牌匾 5s 谢幕（埋点实测）；判定纯函数 eventCardBaseline 带 9 条时序单测
- **📖 说书人开场**（StorytellerIntro）：进站先看最新回目"前情提要"，读过不重演
- 顺带供给保障（F）：generateRecap 失败重试一次（一天仅一发）、director 失败日志升 error、解析抽纯函数 parseRecapOutput+测试

流程：SDD 三层审查（逐任务审查+定向复审+整分支终审），全程 0 Critical/0 Important；
终审对 21 条 minor 全部裁定 DEFER-OK。**留档的观察项**（都不紧急）：
- 长驻会话跨过 23:00 cron 后在大事记里读了新回顾，introSeenAt 不推进，下次进站同一回会重演一遍（代价=多点一次关闭）
- 说书人遮罩无键盘焦点陷阱/无 Esc（a11y 打磨项）
- 回顾 tab 开着时落库的新事件会被合并水位线记为已读（30 分钟节奏下窗口极小）
- 流程技巧：验证 5 秒级瞬态 UI 用页内 MutationObserver 埋点，别用截图轮询（工具往返 >6s 必漏拍）

---

## 已完成（2026-08-28）：角色穿墙修复 ✅

按 A 方案手绘碰撞图上线（根因回顾见「已归档」）。这次实际做法与原计划的差异：没用 Tiled GUI，
而是"图像切块标注 → ASCII 碰撞图 → 红色叠加目视校验"的循环，一次做完两档（外墙+全部家具）。

- **唯一事实来源**：`data/collisionMap.txt`（48 行×64 列 ASCII，`#`=挡路 `.`=可走，行=y 列=x）。
  逐格目视绘制：2004 挡路格 / 1068 可走格，可走区域**恰好一个连通分量**（洪泛填充单测锁死，
  门洞、垂花门、大门、楼梯全部连通）。旧 objmap 双层 1165 个错位挡路格全部废弃。
- **以后改碰撞就这四步（不再需要 resetWorld）**：
  1. 编辑 `data/collisionMap.txt`（可用 `scripts/collisionOverlay.py` 的 grid/overlay/paint/stats 辅助）
  2. `node data/convertCollision.mjs`——只重写 `data/gentle.js` 的 objmap 区块
  3. `npm test`——尺寸/取值/逐格一致/连通性/关键点位防呆
  4. `npx convex deploy && npx convex run testing:patchMapCollision --prod`——单事务完成
     改数据 + 救援站在新墙里的角色 + kick 引擎；不重置世界、不丢时间线（原理见 PIT-0015）
- **顺带做掉**：objmap 从 2 层减到 1 层（原第 2 层全空）；`Player.join` 撒点尝试 10→50 次
  （可走格占比 62%→35% 后，10 次全落墙里的概率约 1.4%，会让访客加入偶发报错）。

**遗留观察项（自愈性，暂不处理）**：窄道（垂花门 x25-26、各门洞约 2 格宽）两侧的对话双方
可能互堵 30-60 秒，日志短时间刷 `Failed to route`，随后由邀请/寻路超时自愈。这是既有的
对话趋近机制在真实走廊地形上的暴露，不是本次引入的 bug；若将来观感差，改法是给会合点
选择加 blocked 预检，而不是拓宽碰撞。

---

## 架构决策待想清楚：两套记忆系统是否要合一

**现状（2026-08-27 查证）**：本地记忆和 EverOS 记忆**并行跑，每次对话两套都注入**——生产日志 35 次对话生成里，两个记忆段各出现 35 次，不是二选一。

两者的检索机制完全不同：

| | 本地记忆（Convex） | EverOS 记忆 |
|---|---|---|
| 怎么检索 | 我们算 embedding 向量，`ctx.vectorSearch('memoryEmbeddings', ...)` 库内比对 | 发**明文** query 过去（`method: 'hybrid'`），它自己在服务端检索 |
| 用不用我们的 embedding 模型 | **用**（`fetchEmbedding` 写入时算、`embeddingsCache.fetch` 查询时算） | **不用**，embedding 发生在它那头，算 MemCell 额度 |
| 内容长度 | 短，最长约 420 字 | 长篇叙事，p90 938 字 |
| 失败模式 | 数据库内操作，基本不会失败 | 外部 HTTP，8 秒超时，fail-soft 会**静默返回空** |
| 作用域 | 跟着 world 走 | 跨 world（v1.1 已加 `EVEROS_SCOPE_PREFIX` 隔离 dev/prod） |

**关键事实：EverOS 自带检索，不需要我们的 embedding 模型。** 所以如果哪天退掉本地这套，`LLM_EMBEDDING_MODEL` / `fetchEmbedding` / `memoryEmbeddings` 表可以整个拿掉，少一个模型依赖。

**但现在不建议动**，两者失败模式互补：本地是库内查询、基本不会挂；EverOS 是外部调用，网络抖一下就静默返回空。留着本地这层，等于 EverOS 挂了角色也不会突然失忆。

**真要简化，需要先回答的问题**：
- EverOS 单独扛得住吗？连续观察它的可用率和延迟，静默返回空的频率有多高
- 两套记忆的内容是否大量重叠？如果 EverOS 已经覆盖了本地那几条，本地就是纯冗余；如果各有各的角度，合并会丢信息
- 成本对比：本地是 AstraFlow 的 embedding 调用（每写一条记忆 1 次 + 每轮对话 1 次带缓存），EverOS 是 MemCell 额度，哪个更省要按真实用量算

这是个架构取舍（省一个供应商依赖 vs 少一层兜底），不是顺手能改的，值得单独一次讨论。

---

## v1.1 终审判为「部署后再说」的延后项

按价值排序。都不影响线上运行，随时可做。

**值得做**

- `sanitizeForPrompt` 的截断仍按 UTF-16 切，压中 emoji 边界会产出孤立代理（`convex/util/sanitize.ts:16`）。同一个 bug 在 UI 层（`SpeechBubble.tsx`）已经用 `[...text]` 按 code point 修好了，**这一层没修，两处不一致**。改法照搬 SpeechBubble。
- `generateRecap` 的 JSON 解析没有独立单测（`convex/director.ts`）。安全性目前靠外层 try 兜住，与已有 9 条测试覆盖的 `parseDirectorOutput` 同构但无回归保护。建议抽出 `parseRecapOutput` 纯函数再补测试。
- 章回体回目序号是 LLM 自编的，没有持久计数器，多条回顾之间序号不连贯（可能"第八十三回"后面跟"第十七回"）。不影响排序（`listRecaps` 按 `day` 排），纯观感。修法：prompt 里注入"这是第 N 回，N = 该 world 已有 recap 数 + 1"，或去掉数字只留标题。
- director 子系统的 catch 一律用 `console.log`（`director.ts:97, 213`；`conversation.ts:245`），而 `everosMemoryPrompt` 用 `console.error`。生产上判断"编剧是不是在静默跳过"全靠日志，`log` 级别不进多数平台的 error 告警。

**记着就行，当前无触发路径**

- 关系面板的去重键用中文名而非 `playerId`，未来若有重名访客会被误合并成同一个交往对象。
- `playerDescriptions` 的 `.collect()` 无上限（`convex/world.ts`，v1.1 新增）。该表每访客一行且 vacuum 永不清理，Convex 单次查询有约 1.6 万文档的硬上限，越过后 `playerMemories` 会**抛错而非降级**，角色面板整个挂掉。9 个 AI 角色 + 零星访客不构成风险，**但做推广活动前应改成按 playerIds 逐个 `.get()`**。
- `recentTitles` 去重窗口是 `take(3)`，只按标题去重，识别不了"换个标题还是同一个设定"。低流量下这个窗口可能横跨好几天。**如果发现事件题材单调，先看这里，别先调 temperature。**
- `day` 字段用 UTC 日期，与 cron 的 `hourUTC:15` 隐式耦合（15:00 UTC = 北京 23:00，当前日历日对齐）。**若单独改 cron 时间到北京跨日的时段，day 标签会错位一天。**
- 反思路径（`memory.ts` 的 `reflectOnMemories`）从未被真实触发过——需单角色累计 importance > 500（约 80+ 次对话）。它的失败模式**不是**静默跳过：`chatCompletion` 在 try 之外、`agentRememberConversation` 无 try，失败会让该角色悬挂到 `ACTION_TIMEOUT`（120 秒）才自愈。观察信号是日志里的 `Timing out {"name":"agentRememberConversation"...}`。
- `convex/util/llm.ts:157` 的 `console.log(body)` 会把完整 system prompt 打进日志。加上 EverOS 的 5×1000 字符注入后日志体量显著变大，留意 Convex 日志配额。
- `relationship` 类型的记忆**没有任何代码生成**（只有 schema 定义）。关系面板现在用 `conversation` 类型顶替。若将来真实现了该类型，需重新评估去重键和数据源。详见 PIT-0012。
- 未配 `eslint-plugin-react-hooks`。v1.1 的 hook 顺序是人工核实的，下一个改 `Player.tsx` 的人没有护栏。

---

## 流程改进（下次多 agent 开工前）

v1.1 用了 9 个并行 agent，发生**两起 git 事故**：一起是 index 竞态（一个 agent 的 commit 卷走了另一个 agent 暂存的文件），一起是善后 revert 误删了别人已完成的工作。代码零损失但都靠人工发现。

**在派发指令里写禁令是不够的——两次事故都发生在明令禁用之后。** 下次多 agent 开工前直接上 `git worktree` 给每个 agent 独立工作区，代价是每份都要装 node_modules。详见 PIT-0014。

---

## 已归档（上一版待办，均已完成或作废）

- ~~角色穿墙 + 空气墙~~（渲染整图贴、碰撞读原版 ai-town 遗留 objmap，两套数据互不相干）→ 2026-08-28 手绘碰撞图修复，流程见上方「已完成」，机制见 PIT-0015
- ~~精灵图重复：燕小六与佟湘玉共用 f1~~ → v1.1 用 PixiJS tint 染色区分（`f1b`，`0x9fd8ff`），未新增美术资产
- ~~地图资源 gentle-obj.png 缺失~~ → 已就位
- ~~修改 App.tsx 的 githubUrl 占位符~~ → 已改为实际地址
- ~~大事记功能开发（当时为占位界面）~~ → v1.1 实现，双标签页（剧集回顾 + 事件流）
- ~~角色头像素材、背景音乐素材~~ → 作废：v1.1 确立"不投入时间制作数字资产"的约束
